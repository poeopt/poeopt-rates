{
  "name": "poeopt-rates",
  "version": "0.2.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs"
  },
  "postinstall": "npx playwright install --with-deps",
  "dependencies": {
    "playwright": "^1.47.2"
  }
}
