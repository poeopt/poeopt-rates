{
  "name": "poeopt-rates",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "postinstall": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "playwright": "^1.45.3"
  }
}
